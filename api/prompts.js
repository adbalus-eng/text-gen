export const config = { runtime: 'edge' };

const PROMPTS = [
  {
    name: "Threads投稿",
    prompt: "以下の条件でSNS投稿文を1つ生成してください。テキストのみ出力、説明・前置き・番号・記号は不要。\n\n設定：\n- テーマ：ホストクラブに通う女性の呟き\n- 必須ワード：「担当」または「ホスト」をどちらか必ず含める\n- 金額：100万・200万・300万・400万・500万・600万・700万・800万・900万・1000万の中からランダムに1つ選んで含める（毎回必ず違う金額にする）\n\n文体ルール：\n- 女性らしい柔らかい語り口\n- 自問・受け入れ・肯定の感情を混在させる\n- 感情トーンはランダムに混在（高揚・依存・開き直り・諦念）\n- 毎回違う感情トーンで生成\n\n禁止事項：\n- 時制を特定させない（今なのか過去なのか未来なのか分からない書き方）\n- 「今日」「今月」「先週」「あの頃」など時間軸を特定する表現は使わない\n- 「〜が飛んだ」「〜を置いてきた」などの不自然な表現は使わない\n- 時系列・累計・月単位の表現は一切使わない\n- 体言止めだけで終わる硬い文末は避ける\n\n出力形式：\n- 50字以内\n- 前置き・解説・まとめは不要、本文のみ出力"
  }
];

export default async function handler(req) {
  return new Response(JSON.stringify(PROMPTS), {
    headers: {
      'Content-Type': 'application/json',
      'Cache-Control': 'no-store'
    }
  });
}
